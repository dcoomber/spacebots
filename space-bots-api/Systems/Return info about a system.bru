meta {
  name: Return info about a system
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/systems/{{systemId}}
  body: none
  auth: none
}

auth:bearer {
  token: 
}

script:post-response {
  let data = res.getBody();
  bru.setVar("neighbouringSystemId", data.neighboringSystems[0].systemId);
}
