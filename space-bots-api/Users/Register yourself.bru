meta {
  name: Register yourself
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v1/users/register
  body: json
  auth: none
}

auth:bearer {
  token: 
}

body:json {
  {
    "name": "{{username}}"
  }
}
